#include <stdio.h>


int main(){
	
	int num1[0];
	int num2[0]; 
	int soma;
	int multiplicacao;
	int subtracao;
	
}

	for(i = 0, i >0, i++){
		if num1 < num2;
		printf("A soma deles são %d:", soma);
	elif num1 == num2:
		printf("A multiplicação deles é %d:");
	elif num1 > num2;
		printf("A subtração deles são %d:");
	}
